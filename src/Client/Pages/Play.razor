@page "/play"

@if (GameManager.CurrentTrack is null)
{
    <Loader LoadText="Creating game..." />
}
else
{
    <video autoplay hidden name="media">
        <source src="@GameManager.CurrentTrack.PreviewUrl" type="audio/mpeg">
    </video>
}